declare global {
    namespace App {
        interface Locals {
        }

        interface Platform {
        }

        interface Error {
            message?: string
        }
    }
}

export {};
